<%= form_for(@m_article) do |f| %>
  <% if @m_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@m_article.errors.count, "error") %> prohibited this m_article from being saved:</h2>

      <ul>
      <% @m_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= form_for :m_article do |f| %>
        <p>
          <%= f.label :title %><br>
          <%= f.text_field :title %>
        </p>

        <p>
          <%= f.label :text %><br>
          <%= f.text_area :text %>
        </p>

        <p>
          <%= f.submit %>
        </p>
    <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
